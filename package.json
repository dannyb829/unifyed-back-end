{
    "name":"unifyed-front-end",
    "description":"front-end-for-heroku",
    "engines":{
        "node":"16.x"
    },
    "scripts":{
        "clean":"rm -rf public",
        "build":"cd client && npm install && npm run build && cd ..",
        "deploy": "cp -a client/build/. public/",
        "heroku-postbuild":"npm run clean && npm run build && npm run deploy"
    }

}