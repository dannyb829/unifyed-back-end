{
    "name":"unifyed-front-end",
    "description":"front-end-for-heroku",
    "engines":{
        "node":">= 14"
    },
    "scripts":{
        "clean":"rm -rf public",
        "build":"npm install --prefix client && nom run build",
        "deploy": "cp -a client/build/. public/",
        "heroku-postbuild":"npm run clean && npm run build"
    }

}